apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-routing
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/abdelhaouari/homelab.git"
    path: gitops/manifests/observability-routing
    targetRevision: main
  destination:
    server: "https://kubernetes.default.svc"
    namespace: ingress-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
