apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-ahaouari-tls
  namespace: ingress-system
spec:
  secretName: nginx-ahaouari-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - nginx.ahaouari.com
